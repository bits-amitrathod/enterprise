<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="project_timesheet_forecast_report_view_pivot" model="ir.ui.view">
         <field name="name">timesheet.forecast.report.pivot</field>
         <field name="model">project.timesheet.forecast.report.analysis</field>
         <field name="arch" type="xml">
             <pivot string="Timesheet Forecast Analysis">
                 <field name="date" interval="month" type="col"/>
                 <field name="employee_id" type="row"/>
                 <field name="number_hours" type="measure"/>
             </pivot>
         </field>
    </record>

    <record id="project_timesheet_forecast_report_view_graph" model="ir.ui.view">
        <field name="name">timesheet.forecast.report.graph</field>
        <field name="model">project.timesheet.forecast.report.analysis</field>
        <field name="arch" type="xml">
            <graph string="Timesheet Forecast Analysis" type="bar">
                <field name="date" type="row"/>
                <field name="number_hours" type="measure"/>
            </graph>
        </field>
    </record>

    <record id="project_timesheet_forecast_report_view_search" model="ir.ui.view">
        <field name="name">timesheet.forecast.report.search</field>
        <field name="model">project.timesheet.forecast.report.analysis</field>
        <field name="arch" type="xml">
            <search string="Timesheet Forecast Analysis">
                <field name="employee_id" filter_domain="[('employee_id', 'ilike', self)]"/>
                <field name="project_id" filter_domain="[('project_id', 'ilike', self)]"/>
                <field name="task_id" filter_domain="[('task_id', 'ilike', self)]"/>
                <field name="date"/>
                <filter string="This Month" name="month" domain="[('date','&gt;=',time.strftime('%%Y-%%m-01'))]"/>
                <filter string="This Year" name="year" domain="[('date','&lt;=', time.strftime('%%Y-12-31')),('date','&gt;=',time.strftime('%%Y-01-01'))]"/>
                <separator/>
                <group expand="1" string="Group By">
                    <filter string="By person, by task" name="resource_task" context="{'group_by':['employee_id', 'task_id']}"/>
                    <filter string="By project, by person" name="project_resource" context="{'group_by':['project_id', 'employee_id']}"/>
                    <separator/>
                    <filter string="By year" name="date_year" context="{'group_by':['date:year']}"/>
                    <filter string="By month" name="date_month" context="{'group_by':['date:month']}"/>
                    <filter string="By week" name="date_week" context="{'group_by':['date:week']}"/>
                    <filter string="By day" name="date_day" context="{'group_by':['date:day']}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="project_timesheet_forecast_report_action" model="ir.actions.act_window">
        <field name="name">Timesheet Forecast Analysis</field>
        <field name="res_model">project.timesheet.forecast.report.analysis</field>
        <field name="view_mode">graph,pivot</field>
        <field name="search_view_id" ref="project_timesheet_forecast_report_view_search"/>
        <field name="context">{
            'pivot_row_groupby': ['employee_id'],
            'pivot_col_groupby': ['date', 'type'],
            'pivot_measures': ['number_hours'],
            'group_by': ['date:month', 'type'],
            'search_default_year': True,

        }</field>
        <field name="help">This report performs analysis on your timesheet and forecast. This allow to have a global view of past forecast and what was really done. Comparing old estimation and effective work amount will help you to better plan your futur project.</field>
    </record>

    <menuitem id="menu_project_timesheet_forecast_report_analysis"
        name="Timesheet and Forecast Analysis"
        parent="project.menu_project_report"
        action="project_timesheet_forecast_report_action"/>

</odoo>
